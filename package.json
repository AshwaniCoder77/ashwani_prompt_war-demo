{
  "name": "flow-venue-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend-api && npm install && cd ../mobile-app && npm install && npm run build && node -e \"require('fs').cpSync('dist', '../backend-api/public', {recursive:true, force:true})\"",
    "start": "cd backend-api && node index.js",
    "test": "cd mobile-app && npm test && cd ../backend-api && npm test"
  },
  "dependencies": {
    "puppeteer": "^24.41.0"
  }
}
