{
  "name": "flow-venue-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd mobile-app && npm install && npm run build",
    "start": "cd backend-api && node index.js"
  }
}
